# Provider: aws

Stub Terraform — sem `.tf` nesta rodada.

Workload concreto (lab-min) está no plano: [`../../docs/AWS_IAC_PLAN.md`](../../docs/AWS_IAC_PLAN.md).  
1 EC2 compose + agent serverless `min=0`. Apply só com `aws_account_id` Luure.

Quando houver artefato deployável, adicionar módulos/overlays aqui e registrar em `../../inventory.md`.
