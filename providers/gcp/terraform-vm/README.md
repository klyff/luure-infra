# LEGADO — terraform-vm (voce-br)

Não use para lab novo. Stack vigente: [`../lab-min/`](../lab-min/).

Este diretório provisiona a VM única antiga (`voce-br-vm`, `e2-standard-4`, `pd-ssd` 200 GB, VPC `default`, labels `smartecm.io`). Mantido só como histórico.

Ver [`../LEGACY.md`](../LEGACY.md).
